# GitHub-hosted preview images not yet recognized by actionlint 1.7.12.
# Remove these entries once actionlint includes the labels.
# https://github.com/actions/runner-images/issues/14226
self-hosted-runner:
  labels:
    - ubuntu-26.04
    - ubuntu-26.04-arm
